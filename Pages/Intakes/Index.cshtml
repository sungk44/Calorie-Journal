@page
@model Calorie_Journal.Pages.Intakes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Intake[0].DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intake[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intake[0].FoodEntry)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intake[0].FoodEntry.Calories)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intake[0].FoodEntry.Carbs)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Intake) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodEntry.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodEntry.Calories)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodEntry.Carbs)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
